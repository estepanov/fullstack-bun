# Docker Compose Environment Variables
# Copy this file to .env and update with your values

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=mydatabase
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PORT=6379
REDIS_PASSWORD=changeme

# Application Ports
API_PORT=3001
FRONTEND_PORT=5173
FRONTEND_HMR_PORT=5174

# Production Configuration
# CORS_ALLOWLISTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# VITE_API_URL=https://api.yourdomain.com

# Note: For production, update the following with secure values:
# - POSTGRES_PASSWORD (use a strong random password)
# - REDIS_PASSWORD (set and store securely; required for Redis access)
# - CORS_ALLOWLISTED_ORIGINS (set to your production domain)
# - VITE_API_URL (set to your production API URL)
